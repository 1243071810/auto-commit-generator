{
  "name": "auto-commit-generator",
  "version": "1.0.0",
  "description": "自动根据 git staged changes 生成 commit message",
  "type": "module",
  "scripts": {
    "start": "node auto-commit.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0"
  }
}
